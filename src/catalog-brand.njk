---
layout: base.njk
pagination:
  data: brands
  size: 1
  alias: brand
permalink: "/catalog/{{ brand.slug }}/"
eleventyComputed:
  title: "{{ brand.name }} — Каталог — Thermo Concept Engineering"
---
{# 14.08. Страницы-заглушки брендов каталога, по одной на каждую
   запись из src/_data/brands.js (9 штук: /catalog/arbonia/,
   /catalog/elco/ и т.д.). Генерируются пагинацией size:1 — тот же
   список, по которому строится и сетка карточек в catalog-l.njk, так
   что ссылка "Смотреть" из карточки гарантированно ведёт на реально
   существующую страницу и они не разъезжаются.

   Содержимое — намеренно та же заглушка, что была у самой /catalog/
   до вёрстки (секция .hero с названием страницы) и что сейчас у
   остальных ещё не свёрстанных внутренних страниц. Свёрстанный
   контент бренда — отдельная задача, макетов пока нет; здесь только
   рабочий адрес + правильный <title>, чтобы каталог не вёл в 404.

   Хвост страницы (Brands/CTA/Footer) — стандартный из base.njk, как
   и у прочих заглушек: customSections тут НЕ выставлен. #}
<section class="hero" data-figma-node="308:1917" data-figma-name="Hero">
  <p class="hero__title">{{ brand.name }}</p>
</section>
