---
title: О нас — Thermo Concept Engineering
layout: base.njk
stickyHeader: true
headerTheme: light
hasVideoUpgrade: true
hasGallery: true
---
{# 10.08: первые по-настоящему свёрстанные секции страницы /about/ —
   Hero - L (см. hero-l.njk, node 1130:9467), Features - L (см.
   features-l.njk, node 1260:17615, добавлена 11.08), Team - L (см.
   team-l.njk, node 1149:12324) и Docs - L (см. docs-l.njk, node
   1172:14382 — пересобрана 11.08 на основе Projects - L с Home).
   Хвост страницы
   (Brands/Divider/CTA/Footer) пока НЕ трогается — customSections тут
   сознательно НЕ выставлен, страница по-прежнему получает стандартный
   хвост из base.njk (divider → brands → divider → cta →
   divider(edge-secondary) → footer), как и другие ещё не полностью
   свёрстанные внутренние страницы (Services/Каталог). Если разметка
   хвоста по Figma этой страницы (node 1130:8714, "About") в будущем
   потребует отличий — дорабатывать отдельно, вне текущей задачи.

   stickyHeader — тот же приём, что у /contact/ и юридических страниц
   (см. подробный комментарий в contact.njk и у .site-header в
   style.2.css): Header становится sticky вместо fixed, поэтому
   отдельный "Header Line"-спейсер (как в Figma-макете этой страницы,
   node 1130:8716) НЕ нужен — под sticky-хедером физически нечему
   обнажаться при overscroll на мобилке, эффект тот же, реализация
   проще. hasVideoUpgrade — подключает hero-video-upgrade.js (см.
   base.njk), нужен для прогрессивной LQ→HQ подгрузки видео внутри
   Hero - L. hasGallery — подключает gallery.js, нужен для карусели
   Docs - L (тот же скрипт, что у Projects - L на Home). #}
{% set heroLBadge = "О нас" %}
{% set heroLHeadingAccent = "Знаем своё дело." %}
{% set heroLHeadingMuted = "10 лет опыта." %}
{% set heroLVideoSrc = "/videos/about-hero.mp4" %}
{% set heroLVideoHq = "/videos/about-hero-hq.mp4" %}
{% set heroLVideoPoster = "/videos/about-hero-poster.jpg" %}
{% include "hero-l.njk" %}
{% set heroLBadge = "" %}
{% set heroLHeadingAccent = "" %}
{% set heroLHeadingMuted = "" %}
{% set heroLVideoSrc = "" %}
{% set heroLVideoHq = "" %}
{% set heroLVideoPoster = "" %}
{# 11.08: Features - L (node 1260:17615) — 3 карточки-принципа "Как мы
   работаем.", сразу под Hero - L. Divider - L ДО и ПОСЛЕ секции — по
   Figma Layout (node 1130:8714) видно на 1440px явно (Hero → Divider →
   Features → Divider → Team), на меньших брейкпоинтах в самом Figma
   Divider-инстансы физически не расставлены (это баг/недоделка
   мокапа), но у самого компонента Divider - L уже есть своя
   адаптивная высота 64→80→96px на все брейкпоинты — используем его
   единообразно везде, а не только с 1440px. #}
{% set dividerVariant = "light" %}
{% include "divider.njk" %}
{% set dividerVariant = "" %}
{% include "features-l.njk" %}
{% set dividerVariant = "light" %}
{% include "divider.njk" %}
{% set dividerVariant = "" %}
{# 11.08: Team - L (node 1149:12324) — сетка фото команды, сразу под
   Divider - L (после Features - L). Раньше между Team - L и Docs - L
   тоже стоял Divider - L — убран по отдельной просьбе пользователя
   (Docs - L идёт сразу следом, у обеих секций свой border, разрыва не
   возникает); эта пара дивайдеров вокруг Features - L — отдельный,
   не связанный случай. #}
{% include "team-l.njk" %}
{# 11.08: Docs - L (node 1172:14382) — карусель сканов документов
   (аттестаты + свидетельство на товарный знак), сразу под Team - L.
   Своей нижней границы (border-bottom) достаточно перед стандартным
   хвостом base.njk (Brands/Divider/CTA/Footer) — в самом Figma (node
   1130:8714, полная страница About) между Docs - L и Brands - D тоже
   нет отдельного дивайдера, только собственные border-bottom секций
   подряд. #}
{% include "docs-l.njk" %}
