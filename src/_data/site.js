/* 17.08. Общие данные сайта для SEO/Open Graph мета-тегов (см. <head> в
 * _includes/base.njk).
 *
 * origin — СХЕМА + ДОМЕН без завершающего слэша и БЕЗ pathPrefix.
 * Нужен именно абсолютный адрес: соцсети и мессенджеры (Telegram,
 * WhatsApp, VK, Slack, Facebook, X) при разборе og:image и og:url
 * относительные пути НЕ раскрывают — превью просто не построится. Всё
 * остальное на сайте по-прежнему собирается фильтром `url`, который сам
 * подставляет pathPrefix; абсолютные ссылки строятся как
 * site.origin + ('/path' | url), поэтому при переезде на свой домен
 * достаточно поменять origin ЗДЕСЬ и pathPrefix в eleventy.config.js — в
 * шаблонах правки не нужны. Сейчас сайт живёт на GitHub Pages в репозитории
 * nixroll/tce-website, отсюда origin + pathPrefix "/tce-website/" дают
 * https://nixroll.github.io/tce-website/.
 *
 * description — дефолтный текст сниппета (<meta name="description"> и
 * og:description) для страниц, которые не задали свой. Собран из
 * формулировок, которые уже есть на самом сайте (Hero/Services/Brands),
 * чтобы не расходиться с ним: «Полный цикл. От расчёта и проекта до
 * пусконаладки и сервиса», «Мировые производители. Напрямую». Текст
 * маркетинговый — стоит вычитать и поправить под себя, механика от него
 * не зависит.
 *
 * Страница может переопределить и заголовок сниппета, и описание через
 * front matter: description: "...". */
module.exports = {
  /* 18.08: origin теперь зависит от режима сборки — тем же флагом
     GITHUB_PAGES, что и pathPrefix (см. подробный разбор в
     eleventy.config.js). Иначе на боевом домене og:image и canonical
     указывали бы на превью-адрес: соцсети тянули бы обложку с чужого
     хоста, а поисковик считал бы каноничной страницу на github.io и
     показывал бы в выдаче её вместо thermoconcept.by. */
  origin:
    process.env.GITHUB_PAGES === "true"
      ? "https://nixroll.github.io"
      : "https://thermoconcept.by",

  /* 18.08. Признак черновой сборки. Нужен, чтобы закрыть превью на
     GitHub Pages от поисковиков: содержимое там то же, что на боевом
     домене, а canonical указывает сам на себя — то есть для Google это
     полноценный второй сайт с дублирующим контентом. Такой дубль умеет
     и оттягивать позиции у настоящего домена, и просто попадать в
     выдачу вместо него. См. <meta name="robots"> в base.njk. */
  isDraft: process.env.GITHUB_PAGES === "true",
  name: "Thermo Concept Engineering",
  locale: "ru_RU",
  /* 21.08, SEO-аудит (пользователь): тире в середине фразы убрано —
     проще и понятнее для сниппета в поиске. */
  description:
    "Проектируем, поставляем и монтируем инженерные системы и котельные под ключ: от расчёта до сервисного обслуживания. Оборудование мировых производителей напрямую.",
};
